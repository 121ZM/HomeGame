# 2026-09-23

## 修正昨天的「封面圆角」修法（用户实拍第二次揪出来）

用户贴了磁贴左上角的放大截图：「圆角不对劲」。放大像素采样后确认：
**封面色块把磁贴的墨色描边整个盖住了** —— 描边只剩一条抗锯齿的灰毛边
（实测角部描边像素是 `c9c7be`/`84827a` 的浅灰，而不是 `221f1a` 的墨色），
圆角弧线跟着糊掉。

**昨天的修法（磁贴 `clip_children = CLIP_CHILDREN_AND_DRAW`）是错的理解：**
`clip_children` 只能切掉「出界」的部分（方角冒出圆角外），但 Godot 里
**子节点永远画在父节点 StyleBox（含描边）之上** —— 封面铺满内容区
（`content_margin=0`），把描边压在身下，裁剪根本救不了描边。

**正确修法 = 照 CSS 语义直译**（`hud.gd:_cover_slot()`）：
1. 封面板四边**收进描边内壁** `STROKE`（3px）：`offset_left/top = STROKE`、
   `offset_right = -STROKE`（底部不收，由那条墨色收边线收口）——
   通栏的「贴边」效果不变，因为色块紧贴描边内侧。
2. 封面板**自己带同心圆角**：上面两个角 `TILE_RADIUS - STROKE`（28−3=25，
   与外圆角 28 同心），下面两个角 0。
3. **裁剪下放到封面层**：`plate.clip_children = CLIP_CHILDREN_AND_DRAW` ——
   玩法画的封面色块被裁成圆角，而磁贴的描边在这一层之上、谁也盖不到。
4. 删掉磁贴层的 `clip_children`（`ui_kit.gd:tile()`），注释说明为什么不能放那。

**实拍确认**：角上是清晰的墨色描边 + 干净的同心弧，粉色封面贴着描边内壁走。

**通用教训（取代昨天的说法）**：
- Godot 里「圆角容器 + 铺满子色块」要圆角，**不能只靠父级 `clip_children`** ——
  它管不住「子节点盖住父级描边」。正确姿势：子块收进描边内壁 + 自带同心圆角 +
  裁剪放在子块那一层。
- 内弧半径 = 外圆角 − 描边宽（Godot 的 StyleBoxFlat 描边画在弧内侧，
  `flat_box` 不会自动帮你算）。
- **编辑器没开时也能实拍**：直接 `godot --path . -- --simulate --sim-players=4`
  拉起游戏（要带用户图形会话的环境变量：`DISPLAY/WAYLAND_DISPLAY/XAUTHORITY/
  XDG_RUNTIME_DIR/DBUS_SESSION_BUS_ADDRESS`，从 `plasmashell` 的
  `/proc/<pid>/environ` 读），再 `spectacle -b -n -o <file>` 全屏截图 +
  Pillow 按奶油纸特征色定位窗口裁剪放大。Wayland 不认 `--position`，
  窗口位置要用特征色扫描找。

## 验收

- 自检 239 通过 / 0 失败（退出码 0，哨兵 171 一致）
- 无头跑局 0 错误 / 0 警告
- 实拍对比：修前灰毛边假圆角 → 修后墨色描边真圆角

---

## 拔河 TUG-01 / TUG-02：玩法改「往上蹦」+ 对局屏重构

### TUG-01 · 输入通道从「挥动 + 倾斜」换成「连续蹦」
用户拍板：「倒计时 5 秒 / 对局屏只留顶部一条 / 传感器拉手机改成往上蹦」。
追问后明确三件事：**倒计时 5 秒**；**对局屏只要顶部细条（时间+比分）**；
**「往上蹦」= 连续蹦、越密拉力越大**（不是「一次动作 = 一记拉力」）；
**去掉「持续倾斜 = 慢慢蹭」通道，只留蹦**。

实现（`input/motion_filter.gd`）：
- 挥动检测改成**只认向上**：`accel.y` 高出「自身慢速均值」才算向上。
- 新增 `jump: float`（连续强度）—— 冲高时取 max，不蹦按 `JUMP_DECAY_PER_SEC * dt` 衰减。
- `_last_ms` 自记算 dt（首包 dt=0）。
- 事件 `{"type":"swing"}` → `{"type":"jump"}`；删 `swing_power`。
- `tilt` / `calibrate()` / `reset()` **保留**（倾斜能力没删，见下）。

**⚠️ 密度反转 bug（自检抓出来的，会漏到真机）**：
新增「同冲量越密越强」判据首跑**反了**（密 0.00 vs 疏 0.11）。
根因：慢速均值被密集冲量的尾巴抬高 → 冲量（`accel.y` − 均值）变小 → 反而冲不过阈值。
即「小孩蹦越快越推不动」。修法：**冲高期冻结基线；非冲高时基线向下跟快、向上跟极慢**
（`JUMP_EMA_RISE := 0.0005`）。修后 8 秒实测：密 0.87 / 中 0.58 / 疏 0.27，单调正确。

玩法侧（`minigames/tug/tug_of_war.gd`）：删 `PULL_PER_SWING/SWING_POWER_FLOW/TILT_PULL_PER_SEC`，
新增 `JUMP_PULL_PER_SEC := 0.30`，`tick()` 合并成一行
`rope += dir * JUMP_PULL_PER_SEC * s.jump() * delta`；`pulls` → `jumps`。
`core/game_flow.gd`：`COUNTDOWN_SEC := 3.0` → **`5.0`**。
`sim/simulator.gd` + `main.gd`：`enable_swing/swing_period/SWING_DECAY` → jump 命名。
同步改：`player_session.gd`（加 `jump()`）、`minigame_selftest.gd`、`fake_game.gd`、`tug_selftest.gd`、`selfcheck.gd`。

**倾斜通道现状（用户问过）**：拔河**已不消费**倾斜（常量删除、`tick()` 不读 `tilt()`），
但输入层能力完整保留。建议**先不加回** —— 倾斜一旦有收益，「歪着拿住不动」就是最优解，蹦就废了。
更好的用法是让它和蹦**分工**（倾斜控方向、蹦控拉力），但那是另一个玩法。

### TUG-02 · 对局屏重构（去左栏 + 顶部细横条 + 相机重取景）
用户实拍反馈原话：「**整个游戏画面都被文字挡住了，只保留上方一条显示状态和在线用户，预先说明什么的都去除**」。

`scripts/ui/hud.gd`：
- 删 `_panel_box/_main_card/_main_box/_roster_card/_roster_box` + `_update_roster()`；
  `_fill_roster()` 删逻辑**留空壳**并写清「曾经有什么、为什么没了」（项目先例：外面可能还有引用）。
- 新增 `PLAY_BAR_H := 96.0` + `_build_play_bar()` + `_update_play_bar()`：
  锚顶部、左右留 `MARGIN`、**靠左**、`PILL_RADIUS` 药丸条，内边距上下 14 / 左右 28
  （比大厅状态条 20/34 各收一档才配得上「细」）。
  **横条不居中** —— 绳子远端落在画面上部中间，居中会正好压到它。
  `SHRINK_BEGIN` 让药丸**只占内容宽**，不铺满整行（铺满会像「一条横杠压住场地」）。
- `_stage_margins()`：`offset_left` → **0**（没左栏要躲了）；`offset_top` → `MARGIN + PLAY_BAR_H + GAP`。
- 倒计时文案 → 「拿稳手机，准备往上蹦！」。
- 比分两队数字各上队色 `PlayerPalette.color_for_team(0/1)`，间隔号/队名用 `INK`
  （色弱或队色被地面反光冲淡时也认得出哪个数字是哪队）。
- `_pick_time()`/`_pick_scores()` 从 `status_text()` **宽松抠**出「剩 N 秒」和两队数字
  —— **不写 `is TugOfWar`，守「玩法可插拔」铁律**；抠不到就整行摆进时间格兜底。

**额度提示改图标（用户选择「保留但简化成图标」）—— 零破坏接口**：
- `core/minigame.gd` **纯新增** `status_badges() -> Array`（默认 `[]`）；
  `status_meta() -> String` **签名不动、留作兜底**。
- 元素形如 `{"icon":"revive"|"item","n":次数}`。
- HUD：`status_badges()` 非空 → 画图标+一位数字（❤1 ★1，用系统字体字形，**未新增字体资源**）；
  否则退回 `status_meta()` 整句。`n <= 0` 跳过 → 没额度时不占宽。
- **唯一调用点就是 HUD**，已 grep 确认结算/日志无引用。

`scripts/minigames/tug/tug_field.gd`（全是**纯视觉量**，规则数值一个没动）：
- `camera_view()`：`(-1.35, 3.80, 3.08)`/fov55 → **`(0.0, 2.90, 2.35)`**/俯角 −38°/fov62
  （回正中 + 拉近 + 加宽）。
- `ROPE_HALF 1.6→2.6`（+63%）、`ZONE_HALF 2.4→3.6`（+50%）、`ZONE_DEPTH 5→7`、
  `ZONE_SHIFT_Z −0.6→−0.8`、`POST_H 0.45→0.62`。合起来场地约 **1.5–1.6 倍**。
- 重写「相机偏左躲左栏」注释（x 偏移的理由没了）。

### 自检体系的盲区修复（本轮最大意外收获）
`selfcheck.gd` 里 `rd["strip"]` 这个键名**早就失效**了 —— 大厅药丸搬进状态条时就没了。
症状：**每跑必抛 SCRIPT ERROR 并静默吃掉 HUD 契约后半段约 18 条断言**，哨兵数太小没抓到。
改回真实键名 `seats` 后断言全回来。**框架断言 173 → 197**（哨兵 `EXPECTED_MIN` 172 → **195**）。
教训：**键名写错 + 异常被吞 + 哨兵太松 = 静默盲区**，三个条件凑齐就没人发现。

### 验收 & 环境踩坑
- 自检 **269 通过 / 0 失败**（框架 197 + 拔河 72），哨兵 `EXPECTED_MIN := 195`。
- import 退出码 0；无头跑局 0 错误。
- **⚠️ `-- --selftest` 这个 flag 项目里根本不存在**（全仓 grep 无处理点）——
  误用会进正常游戏、卡在「等待手机连接」直到超时（退出码 124）。
  **正确入口 = `res://tools/selfcheck.tscn`**：
  `godot --headless --path . res://tools/selfcheck.tscn`。
- **⚠️ `pkill -f autostart=tug` 会连带杀掉自己的 shell**（命令行含该串）。
  起模拟对局用 `setsid nohup ... < /dev/null &`，停用 `pkill -f "Godot.*HomeGame"`。
- **实拍路径（编辑器没开也能用）**：
  ```
  setsid nohup godot --path . --resolution 1280x720 -- \
      --simulate --sim-players=2 --autostart=tug > run.log 2>&1 < /dev/null &
  export XDG_RUNTIME_DIR=/run/user/1000 DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1000/bus WAYLAND_DISPLAY=wayland-0
  spectacle -b -n -o shot.png      # 1920x1080 全屏，游戏窗口居中，无需裁剪
  ```
  `spectacle` 会打 tesseract 语言警告，无害。
- 归档：`docs/ui-shots/play-countdown.png`、`play-playing.png`。
- **MCP 实拍（编辑器开着时）**：`session_activate` → `project_run(mode="main")` →
  `editor_manage(op="game_eval", ...)`。`game_eval` 不支持嵌套函数/try-except、
  不会自动补缩进；窗口切后台会 `EVAL_GAME_NOT_READY`（stop → run 重开新窗口）。
  但**跑模拟对局还是命令行路径更省事**（MCP 起的进程没有 `--simulate`，没有 Simulator 节点）。
- 未 git commit（按约定等用户确认）。
